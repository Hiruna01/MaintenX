[pytest]
# Modules are flat inside agent/, so agent/ itself goes on the path. Run tests with
# `pytest` from this directory.
pythonpath = .
testpaths = tests

# Async tests need no per-test decorator.
asyncio_mode = auto

filterwarnings =
    ignore::DeprecationWarning
