# Agent service dependencies. Install into a virtualenv:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7,<3.0
pydantic-settings>=2.3

# Graph orchestration. graph.py is the only file that imports it.
langgraph>=0.2,<0.7

# Tests
pytest>=8.0
pytest-asyncio>=0.23
